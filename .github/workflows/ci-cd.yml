name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  # Backend Tests
  backend-test:
    name: Backend Tests
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('backend/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    - name: Install dependencies
      run: |
        cd backend
        pip install -r requirements.txt
    
    - name: Run linting
      run: |
        cd backend
        flake8 app --count --select=E9,F63,F7,F82 --show-source --statistics
        black --check app
    
    - name: Run tests
      run: |
        cd backend
        pytest tests/ -v --cov=app --cov-report=xml
    
    - name: Upload coverage
      uses: codecov/codecov-action@v3
      with:
        file: ./backend/coverage.xml
        flags: backend
        name: backend-coverage

  # Backend Build
  backend-build:
    name: Backend Docker Build
    runs-on: ubuntu-latest
    needs: backend-test
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Build Docker image
      run: |
        docker build -t edupredict-backend:latest ./backend
    
    - name: Test Docker image
      run: |
        docker run -d --name test-backend -p 8000:8000 edupredict-backend:latest
        sleep 10
        curl -f http://localhost:8000/api/v1/health || exit 1
        docker stop test-backend

  # Frontend Tests (when implemented)
  # frontend-test:
  #   name: Frontend Tests
  #   runs-on: ubuntu-latest
  #   
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
  #   
  #   - name: Set up Node.js
  #     uses: actions/setup-node@v4
  #     with:
  #       node-version: '20'
  #   
  #   - name: Install dependencies
  #     run: |
  #       cd frontend
  #       npm ci
  #   
  #   - name: Run linting
  #     run: |
  #       cd frontend
  #       npm run lint
  #   
  #   - name: Run tests
  #     run: |
  #       cd frontend
  #       npm test -- --coverage
  #   
  #   - name: Build application
  #     run: |
  #       cd frontend
  #       npm run build

  # Security Scan
  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: '.'
        format: 'sarif'
        output: 'trivy-results.sarif'
    
    - name: Upload Trivy results to GitHub Security
      uses: github/codeql-action/upload-sarif@v2
      with:
        sarif_file: 'trivy-results.sarif'

  # Deployment (placeholder for production)
  # deploy:
  #   name: Deploy to Production
  #   runs-on: ubuntu-latest
  #   needs: [backend-build, frontend-test]
  #   if: github.ref == 'refs/heads/main'
  #   
  #   steps:
  #   - name: Deploy to AWS/GCP
  #     run: |
  #       echo "Deployment steps here"
